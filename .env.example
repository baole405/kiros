# Kiros Triage Hub - Environment Variables
# Copy this file to .env before running docker-compose

# Groq API Key (Required for AI Analysis)
# Get one here: https://console.groq.com/keys
LLM_API_KEY=your_groq_api_key_here

# Optional Overrides (Defaults set in docker-compose.yml)
# PORT=4000
# DATABASE_URL=postgres://postgres:password@localhost:5433/kiros_triage
